app = "jamaat-backend"
primary_region = "yyz"

[build]

[env]
  PORT = "8080"
  DATABASE_PATH = "/data/prayer_times.db"
  ALLOWED_ORIGINS = "https://jamaat-chi.vercel.app"

[[mounts]]
  source = "jamaat_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
